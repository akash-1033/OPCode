#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        string n;
        cin>>n;
        ll k = n.length();
        ll s = stoll(n);
        for(ll i=1; i<=9; i++)
        {
            for(ll i=1; i<)
        }
    }
}